name: wenets_dist
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - tqdm
  - pyyaml
  - click
  - pip
  - pip:
      - sox
      - torch
      - torchvision
      - torchaudio
      - pandas
